# Copy to output_policy.env on both nodes. Never commit the populated file.
# Set a rule to 0 to disable it, then restart the service.
SGLANG_HIDE_THINKING=0
SGLANG_TEXT_ONLY=1
SGLANG_WEB_SEARCH=0
SGLANG_STRIP_ATTRIBUTION=1

# Required only when SGLANG_WEB_SEARCH=1.
TAVILY_API_KEY=
